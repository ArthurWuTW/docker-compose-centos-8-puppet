version: '3'
services:
  puppet-master: #service name
    build: 
      context: image/puppet-server
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup
    # command: bash -c "service chronyd start && service puppetserver start"
  puppet-slave1: 
    build: 
      context: image/puppet-agent
    privileged: true