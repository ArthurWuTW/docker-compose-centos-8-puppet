version: '3'
services:
  
  puppet-db:
    build:
      context: image/puppet-db
    privileged: true
    hostname: puppetdb.example.com
    environment:
      - PUPPERWARE_ANALYTICS_ENABLED=false
      - CERTNAME=puppetdb.example.com
      - DNS_ALT_NAMES=false
      - PUPPETSERVER_HOSTNAME=puppetdb.example.com
      - PUPPETSERVER_PORT=8140
      - PUPPETDB_POSTGRES_HOSTNAME=10.1.1.9
      - PUPPETDB_POSTGRES_DATABASE=testdb
      - PUPPETDB_POSTGRES_PORT=5432
      - PUPPETDB_USER=puppetdb
      - PUPPETDB_PASSWORD=bbb
    ports:
      - 8080:8080
      - 8081:8081
      - 8140:8140
    extra_hosts:
      - "puppetdb.example.com:10.1.1.9"
    entrypoint: /sbin/init
  
  puppet-slave1: 
    build: 
      context: image/puppet-agent
    privileged: true
    extra_hosts:
      - "puppetServer.example.com:10.1.1.9"

  postgres-db:
    build:
      context: image/postgres-db
    privileged: true
    ports:
      - "${HOST_PORT_DB}:5432"
